<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>Bibliotheca Admin</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Minimal admin-specific styles */
        .admin-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 40px 24px;
        }
        .admin-header {
            background: linear-gradient(135deg, #0f172a, #1e293b);
            color: white;
            padding: 24px 0;
            margin-bottom: 40px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .admin-nav {
            display: flex;
            gap: 24px;
            align-items: center;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 24px;
        }
        .admin-nav a {
            color: white;
            text-decoration: none;
            font-weight: 600;
            padding: 8px 16px;
            border-radius: 6px;
            transition: background 0.2s;
        }
        .admin-nav a:hover {
            background: rgba(255,255,255,0.1);
        }
        .admin-nav .logo {
            font-family: 'Playfair Display', serif;
            font-size: 24px;
            margin-right: auto;
        }
        .alert {
            padding: 16px 24px;
            border-radius: 8px;
            margin-bottom: 24px;
            font-weight: 500;
        }
        .alert-success {
            background: #d1fae5;
            color: #047857;
            border: 1px solid #86efac;
        }
        .alert-error {
            background: #fee2e2;
            color: #b91c1c;
            border: 1px solid #fca5a5;
        }
        .btn {
            padding: 12px 24px;
            border-radius: 8px;
            border: none;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            transition: all 0.2s;
        }
        .btn-primary {
            background: #dc2626;
            color: white;
        }
        .btn-primary:hover {
            background: #b91c1c;
        }
        .btn-secondary {
            background: #475569;
            color: white;
        }
        .btn-secondary:hover {
            background: #334155;
        }
        .btn-danger {
            background: #dc2626;
            color: white;
        }
        .btn-danger:hover {
            background: #991b1b;
        }
        .btn-sm {
            padding: 8px 16px;
            font-size: 14px;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group label {
            display: block;
            font-weight: 600;
            margin-bottom: 8px;
            color: #1e293b;
        }
        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 12px;
            border: 2px solid #e2e8f0;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.2s;
        }
        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            outline: none;
            border-color: #f59e0b;
        }
        .form-group textarea {
            min-height: 120px;
            resize: vertical;
        }
        .form-error {
            color: #dc2626;
            font-size: 14px;
            margin-top: 4px;
        }
        .form-group.has-error input,
        .form-group.has-error textarea {
            border-color: #dc2626;
        }
        .table-container {
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .search-box {
            padding: 24px;
            border-bottom: 1px solid #e2e8f0;
            display: flex;
            gap: 12px;
            align-items: center;
        }
        .search-box input {
            flex: 1;
            padding: 12px;
            border: 2px solid #e2e8f0;
            border-radius: 8px;
            font-size: 16px;
        }
        .pagination {
            padding: 24px;
            display: flex;
            justify-content: center;
            gap: 8px;
            border-top: 1px solid #e2e8f0;
        }
        .pagination a,
        .pagination span {
            padding: 8px 16px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
        }
        .pagination a {
            background: #f1f5f9;
            color: #334155;
        }
        .pagination a:hover {
            background: #e2e8f0;
        }
        .pagination .active {
            background: #dc2626;
            color: white;
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>
    
    <div class="admin-container">
        <%- body %>
    </div>
    
    <%- include('../partials/footer') %>
</body>
</html>
